define(['React', 'components/login/login'],
    function (React, Login) {
        'use strict';

        console.log(Login);

        //var testUtils = React.addons.TestUtils;

        describe('First Describe Block', function () {
            it('should fail', function () {
                expect(1).toEqual(2);
            });
        });

    });
